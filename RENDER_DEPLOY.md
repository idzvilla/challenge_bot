# 📋 Пошаговая инструкция по деплою на Render.com

## 🎯 Что мы делаем:

1. Создаем PostgreSQL базу данных на Render
2. Задеплоим бота на Render
3. Настраиваем переменные окружения
4. Готово! Бот работает и данные сохраняются

---

## 📝 Шаг 1: Регистрация на Render

1. Откройте [render.com](https://render.com)
2. Нажмите **"Get Started for Free"**
3. Зарегистрируйтесь через GitHub (рекомендуется) или email
4. Подтвердите email, если нужно

---

## 🗄️ Шаг 2: Создание PostgreSQL базы данных

1. В Dashboard нажмите **"New +"** → **"PostgreSQL"**
2. Заполните форму:
   - **Name:** `challenge-bot-db` (любое имя)
   - **Database:** `challenge_bot` (или любое другое)
   - **User:** `challenge_user` (или любое другое)
   - **Region:** Выберите ближайший (например, Frankfurt)
   - **PostgreSQL Version:** 16 (или последняя)
   - **Plan:** **Free** (бесплатный тариф)
3. Нажмите **"Create Database"**
4. ⏳ Подождите 1-2 минуты, пока база создается

### Получение Connection String:

1. После создания базы откройте её
2. В разделе **"Connections"** найдите **"Internal Database URL"**
3. Скопируйте этот URL - он выглядит так:
   ```
   postgresql://challenge_user:password@dpg-xxxxx-a/challenge_bot
   ```
4. **Сохраните этот URL** - он понадобится позже!

---

## 🤖 Шаг 3: Создание Web Service для бота

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Выберите **"Build and deploy from a Git repository"**
3. Подключите ваш GitHub аккаунт (если еще не подключен)
4. Выберите репозиторий: **`idzvilla/challenge_bot`**
5. Нажмите **"Connect"**

### Настройки сервиса:

1. **Name:** `challenge-bot` (любое имя)
2. **Region:** Выберите тот же регион, что и для БД
3. **Branch:** `main`
4. **Root Directory:** (оставьте пустым)
5. **Runtime:** `Node`
6. **Build Command:** 
   ```
   npm install && npm run build
   ```
7. **Start Command:**
   ```
   npm start
   ```
8. **Plan:** **Free** (бесплатный тариф)

### Переменные окружения:

Прокрутите вниз до раздела **"Environment Variables"** и добавьте:

1. **BOT_TOKEN**
   - Value: `8545493908:AAFB-7bDNIpDD6p-jTcLon8kyfru--5j7Tg`

2. **DATABASE_URL**
   - Value: Вставьте тот URL, который скопировали из PostgreSQL (Internal Database URL)

3. **TIMEZONE**
   - Value: `Europe/Minsk`

4. **CHALLENGE_START_DATE**
   - Value: `2024-01-01`

**Важно:** НЕ добавляйте `DATABASE_PATH` - он не нужен при использовании PostgreSQL!

### Создание сервиса:

1. Прокрутите вниз
2. Нажмите **"Create Web Service"**
3. ⏳ Подождите 3-5 минут, пока идет сборка и деплой

---

## ✅ Шаг 4: Проверка работы

1. После завершения деплоя откройте ваш сервис
2. Перейдите на вкладку **"Logs"**
3. Ищите сообщения:
   - `Using PostgreSQL database` ✅
   - `Database connection successful` ✅
   - `✅ Bot is running!` ✅

4. Откройте вашего бота в Telegram
5. Отправьте `/start`
6. Бот должен ответить! 🎉

---

## 🔧 Шаг 5: Настройка (опционально)

### Автоматический перезапуск при обновлении кода:

Render автоматически перезапускает сервис при каждом push в GitHub!

### Просмотр логов:

- В Dashboard → ваш сервис → вкладка **"Logs"**
- Логи обновляются в реальном времени

### Мониторинг:

- В Dashboard → ваш сервис → вкладка **"Metrics"**
- Можно видеть использование CPU, памяти и т.д.

---

## 🐛 Решение проблем

### Бот не запускается:

1. Проверьте логи в Render
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что `DATABASE_URL` правильный (Internal Database URL из PostgreSQL)

### Ошибка подключения к БД:

1. Убедитесь, что используете **Internal Database URL** (не External!)
2. Проверьте, что PostgreSQL сервис запущен
3. Проверьте логи PostgreSQL сервиса

### Данные не сохраняются:

1. Убедитесь, что `DATABASE_URL` установлен
2. Проверьте логи - должно быть `Using PostgreSQL database`
3. Проверьте, что PostgreSQL сервис работает

---

## 💡 Полезные ссылки

- [Render Dashboard](https://dashboard.render.com)
- [Render Docs](https://render.com/docs)
- [PostgreSQL на Render](https://render.com/docs/databases)

---

## 🎉 Готово!

Теперь ваш бот работает на Render с PostgreSQL базой данных. Данные будут сохраняться и не потеряются при перезапуске!

**Важно:** На бесплатном тарифе Render сервис может "засыпать" после 15 минут неактивности, но для Telegram-бота это не критично - бот сам инициирует запросы.



