# Быстрая настройка DBeaver для работы с базой данных

## Шаг 1: Скачайте DBeaver

1. Откройте https://dbeaver.io/download/
2. Выберите **DBeaver Community Edition** (бесплатно!)
3. Скачайте версию для вашей ОС (Windows/Mac/Linux)
4. Установите и запустите

## Шаг 2: Получите данные для подключения

1. Откройте Render Dashboard
2. Найдите ваш PostgreSQL сервис (`challenge-bot-db`)
3. Откройте его
4. Перейдите на вкладку **"Info"**
5. Найдите **"External Database URL"**
6. Скопируйте его - он выглядит так:
   ```
   postgresql://challenge_user:password@dpg-xxxxx-a.frankfurt-postgres.render.com:5432/challenge_bot
   ```

## Шаг 3: Создайте подключение в DBeaver

1. В DBeaver нажмите **"New Database Connection"** (иконка вилки вверху)
2. Выберите **PostgreSQL**
3. Нажмите **"Next"**

4. **Заполните данные:**
   - **Host:** `dpg-xxxxx-a.frankfurt-postgres.render.com` (из URL)
   - **Port:** `5432` (или из URL)
   - **Database:** `challenge_bot` (из URL)
   - **Username:** `challenge_user` (из URL)
   - **Password:** пароль из URL (или установите новый в Render)

5. Нажмите **"Test Connection"**
   - Если попросит скачать драйвер - скачайте
   - Если успешно - нажмите **"Finish"**

## Шаг 4: Работайте с данными

1. В левой панели найдите вашу базу данных
2. Разверните: **Databases** → **challenge_bot** → **Schemas** → **public** → **Tables**
3. Вы увидите таблицы:
   - **users** - пользователи
   - **logs** - логи подтягиваний

4. **Просмотр данных:**
   - Правой кнопкой на таблицу → **"View Data"**
   - Или двойной клик на таблицу

5. **Выполнение SQL:**
   - Правой кнопкой на базу данных → **"SQL Editor"** → **"New SQL Script"**
   - Или нажмите `Ctrl+` (Windows/Linux) или `Cmd+` (Mac)
   - Введите SQL запрос и нажмите `Ctrl+Enter` (или кнопку "Execute")

## Полезные SQL запросы:

```sql
-- Посмотреть всех пользователей
SELECT * FROM users;

-- Посмотреть все логи
SELECT * FROM logs ORDER BY logged_at DESC;

-- Статистика по пользователям
SELECT 
  u.tg_user_id,
  u.username,
  u.first_name,
  COUNT(l.id) as total_logs,
  COALESCE(SUM(l.reps), 0) as total_reps
FROM users u
LEFT JOIN logs l ON u.id = l.user_id
GROUP BY u.id
ORDER BY total_reps DESC;

-- Изменить количество подтягиваний
UPDATE logs SET reps = 100 WHERE id = 1;

-- Удалить запись
DELETE FROM logs WHERE id = 1;
```

## Готово! 🎉

Теперь вы можете легко просматривать и изменять данные в базе через удобный графический интерфейс DBeaver.


