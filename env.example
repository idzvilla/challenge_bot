BOT_TOKEN=your_telegram_bot_token_here
TIMEZONE=Europe/Minsk
CHALLENGE_START_DATE=2024-01-01

# Для PostgreSQL (используется если установлен)
# Получите из Render PostgreSQL → Internal Database URL
DATABASE_URL=postgresql://user:password@host:port/database

# Для SQLite (используется если DATABASE_URL не установлен)
DATABASE_PATH=./data/challenge.db
