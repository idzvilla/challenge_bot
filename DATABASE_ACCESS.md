# Как посмотреть и изменить данные в PostgreSQL на Render

## Способ 1: Через DBeaver (БЕСПЛАТНО, GUI) ⭐⭐⭐

**Самый простой и удобный способ!**

1. **Скачайте DBeaver Community Edition** (полностью бесплатно):
   - https://dbeaver.io/download/
   - Выберите версию для вашей ОС (Windows/Mac/Linux)

2. **Установите и запустите DBeaver**

3. **Создайте новое подключение:**
   - Нажмите "New Database Connection" (иконка вилки)
   - Выберите **PostgreSQL**
   - Нажмите "Next"

4. **Получите данные для подключения:**
   - Откройте Render Dashboard → ваш PostgreSQL сервис
   - Перейдите на вкладку **"Info"**
   - Найдите **"External Database URL"** (НЕ Internal!)
   - Скопируйте его

5. **Заполните данные подключения:**
   - **Host:** извлеките из URL (например, `dpg-xxxxx-a.frankfurt-postgres.render.com`)
   - **Port:** обычно `5432` (или из URL)
   - **Database:** имя базы (например, `challenge_bot`)
   - **Username:** пользователь из URL
   - **Password:** пароль из URL (или установите новый в Render)
   - Нажмите "Test Connection"
   - Если успешно - "Finish"

6. **Работайте с данными:**
   - В левой панели найдите вашу базу данных
   - Разверните: Databases → challenge_bot → Schemas → public → Tables
   - Правой кнопкой на таблицу → "View Data"
   - Или используйте SQL Editor (Ctrl+`) для выполнения запросов

---

## Способ 2: Через psql (командная строка, БЕСПЛАТНО) ⭐⭐

### Подключение через External Database URL:

1. **Получите External Database URL:**
   - Render Dashboard → PostgreSQL сервис → вкладка "Info"
   - Скопируйте **"External Database URL"**

2. **Установите psql** (если еще не установлен):
   ```bash
   # macOS
   brew install postgresql
   
   # Linux (Ubuntu/Debian)
   sudo apt-get install postgresql-client
   
   # Windows
   # Скачайте PostgreSQL с https://www.postgresql.org/download/windows/
   # Или используйте WSL
   ```

3. **Подключитесь:**
   ```bash
   psql "postgresql://user:password@host:port/database"
   ```
   
   Замените на ваш External Database URL полностью

4. **Выполняйте SQL команды:**
   ```sql
   \dt                    -- Показать все таблицы
   SELECT * FROM users;   -- Посмотреть пользователей
   SELECT * FROM logs;    -- Посмотреть логи
   \q                     -- Выйти
   ```

---

## Способ 3: Через Render Dashboard (если есть SQL Editor)

1. **Откройте Render Dashboard**
2. **Найдите ваш PostgreSQL сервис** (challenge-bot-db)
3. **Откройте его**
4. **Перейдите на вкладку "Data" или "SQL Editor"** (если доступна)
5. **Выполните SQL запросы:**

```sql
-- Посмотреть всех пользователей
SELECT * FROM users;

-- Посмотреть все логи подтягиваний
SELECT * FROM logs ORDER BY logged_at DESC;

-- Посмотреть статистику по пользователям
SELECT 
  u.tg_user_id,
  u.username,
  u.first_name,
  COALESCE(SUM(l.reps), 0) as total_reps
FROM users u
LEFT JOIN logs l ON u.id = l.user_id
GROUP BY u.id
ORDER BY total_reps DESC;

-- Изменить количество подтягиваний у пользователя
UPDATE logs SET reps = 100 WHERE id = 1;

-- Удалить запись
DELETE FROM logs WHERE id = 1;

-- Добавить подтягивания вручную
INSERT INTO logs (user_id, reps, logged_at, log_date)
VALUES (1, 50, NOW(), CURRENT_DATE);
```

---

## Способ 4: Через pgAdmin (локально, БЕСПЛАТНО)

### Подключение через External Database URL:

1. **Получите External Database URL:**
   - Откройте PostgreSQL сервис в Render
   - Вкладка "Info" или "Connections"
   - Скопируйте **"External Database URL"**

2. **Установите psql** (если еще не установлен):
   ```bash
   # macOS
   brew install postgresql
   
   # Linux
   sudo apt-get install postgresql-client
   ```

3. **Подключитесь:**
   ```bash
   psql "postgresql://user:password@host:port/database"
   ```
   
   Замените на ваш External Database URL

4. **Выполняйте SQL команды:**
   ```sql
   \dt                    -- Показать все таблицы
   SELECT * FROM users;   -- Посмотреть пользователей
   SELECT * FROM logs;    -- Посмотреть логи
   \q                     -- Выйти
   ```

---

## Способ 5: Через онлайн инструменты (БЕСПЛАТНО)

### pgAdmin (локально):

1. **Скачайте pgAdmin:** https://www.pgadmin.org/download/ (бесплатно)
2. **Установите и запустите**
3. **Создайте новое подключение:**
   - Host: из External Database URL
   - Port: из External Database URL
   - Database: из External Database URL
   - Username: из External Database URL
   - Password: из External Database URL
4. **Подключитесь и работайте через GUI**

### Онлайн инструменты:

1. **Скачайте DBeaver:** https://dbeaver.io/download/
2. **Создайте новое подключение:**
   - Тип: PostgreSQL
   - Host, Port, Database, Username, Password из External Database URL
3. **Подключитесь и работайте через GUI**

---

## Способ 6: Через Render CLI (если установлен)

1. **Установите Render CLI:**
   ```bash
   curl -fsSL https://render.com/install.sh | sh
   ```

2. **Войдите:**
   ```bash
   railway login
   ```

3. **Подключитесь к базе:**
   ```bash
   railway connect -s your-postgres-service-name
   ```

---

## Полезные SQL запросы:

### Посмотреть структуру таблиц:
```sql
-- Структура таблицы users
\d users

-- Структура таблицы logs
\d logs
```

### Статистика:
```sql
-- Общее количество пользователей
SELECT COUNT(*) FROM users;

-- Общее количество подтягиваний
SELECT SUM(reps) FROM logs;

-- Подтягивания за сегодня
SELECT SUM(reps) FROM logs WHERE log_date = CURRENT_DATE;

-- Топ-10 пользователей
SELECT 
  u.username,
  u.first_name,
  SUM(l.reps) as total
FROM users u
JOIN logs l ON u.id = l.user_id
GROUP BY u.id
ORDER BY total DESC
LIMIT 10;
```

### Изменение данных:
```sql
-- Изменить количество подтягиваний в записи
UPDATE logs SET reps = 100 WHERE id = 1;

-- Удалить все записи пользователя
DELETE FROM logs WHERE user_id = 1;

-- Удалить пользователя и все его записи (каскадное удаление)
DELETE FROM users WHERE id = 1;

-- Изменить username пользователя
UPDATE users SET username = 'new_username' WHERE tg_user_id = 123456789;
```

### Очистка данных:
```sql
-- Удалить все логи
DELETE FROM logs;

-- Удалить всех пользователей (и их логи автоматически)
DELETE FROM users;

-- Сбросить счетчики (очистить логи, но оставить пользователей)
TRUNCATE TABLE logs;
```

---

## ⚠️ Важно:

1. **Делайте бэкапы перед изменением данных!**
2. **Будьте осторожны с DELETE** - данные удаляются навсегда
3. **Используйте транзакции для сложных операций:**
   ```sql
   BEGIN;
   -- ваши изменения
   COMMIT;  -- или ROLLBACK; для отмены
   ```

---

## 🎯 Рекомендация:

**Для начала используйте DBeaver (Способ 1)** - это самый простой и удобный бесплатный инструмент с графическим интерфейсом!

Если нужен быстрый доступ через командную строку - используйте **psql (Способ 2)**.

