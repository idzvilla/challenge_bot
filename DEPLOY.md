# Инструкция по деплою бота

## 🚀 Бесплатные платформы для деплоя

### 1. Railway (Рекомендуется) ⭐

**Бесплатно:** $5 кредитов в месяц (достаточно для небольшого бота)

**Шаги:**
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Подключите ваш GitHub репозиторий
4. Добавьте переменные окружения в настройках проекта:
   - `BOT_TOKEN` - ваш токен бота
   - `TIMEZONE` - Europe/Minsk (или другая)
   - `CHALLENGE_START_DATE` - 2024-01-01
   - `DATABASE_PATH` - ./data/challenge.db
5. Railway автоматически определит Node.js проект и запустит `npm start`
6. Бот будет работать 24/7

**Преимущества:**
- Очень простой деплой
- Автоматический HTTPS
- Логи в реальном времени
- Бесплатный тариф достаточен для бота

---

### 2. Render

**Бесплатно:** Да, но может "засыпать" после 15 минут неактивности

**Шаги:**
1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте новый "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm start`
   - **Environment:** Node
5. Добавьте переменные окружения (Environment Variables)
6. Деплой запустится автоматически

**Примечание:** На бесплатном тарифе сервис может засыпать, но для Telegram-бота это не критично, так как бот сам инициирует запросы.

---

### 3. Fly.io

**Бесплатно:** Да, 3 shared-cpu-1x виртуальные машины

**Шаги:**
1. Установите Fly CLI: `curl -L https://fly.io/install.sh | sh`
2. Зарегистрируйтесь: `fly auth signup`
3. В директории проекта: `fly launch`
4. Следуйте инструкциям
5. Установите переменные: `fly secrets set BOT_TOKEN=your_token TIMEZONE=Europe/Minsk`
6. Деплой: `fly deploy`

---

### 4. Replit

**Бесплатно:** Да, но с ограничениями

**Шаги:**
1. Зарегистрируйтесь на [replit.com](https://replit.com)
2. Создайте новый Repl → "Import from GitHub"
3. Укажите URL вашего репозитория
4. В разделе "Secrets" добавьте переменные окружения
5. Нажмите "Run"

**Примечание:** На бесплатном тарифе Repl может останавливаться после неактивности.

---

### 5. Oracle Cloud (Always Free)

**Бесплатно:** Да, постоянный бесплатный VPS

**Шаги:**
1. Зарегистрируйтесь на [cloud.oracle.com](https://cloud.oracle.com)
2. Создайте Always Free VM (Ubuntu)
3. Подключитесь по SSH
4. Установите Node.js: `curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash - && sudo apt-get install -y nodejs`
5. Клонируйте репозиторий
6. Установите зависимости и запустите через PM2: `npm install -g pm2 && pm2 start dist/index.js --name bot`

**Преимущества:** Полностью бесплатно, без ограничений по времени

---

## 📝 Подготовка к деплою

### Важно для всех платформ:

1. **Не коммитьте `.env` файл** (уже в `.gitignore`)
2. **Установите переменные окружения** на платформе
3. **Убедитесь, что `DATABASE_PATH` указывает на постоянное хранилище** (на некоторых платформах нужно использовать volume)

### Для Railway и Render:

Эти платформы автоматически определят Node.js проект. Убедитесь, что:
- `package.json` содержит скрипт `start`
- Проект собирается командой `npm run build`

### Для платформ с файловой системой:

Если платформа не сохраняет файлы между перезапусками, рассмотрите использование внешней БД (PostgreSQL) или облачного хранилища для SQLite файла.

---

## 🔧 Альтернатива: Использование PostgreSQL вместо SQLite

Если нужна более надежная БД для продакшена, можно перейти на PostgreSQL. Для этого нужно:
1. Изменить `better-sqlite3` на `pg` (PostgreSQL клиент)
2. Обновить код работы с БД
3. Использовать бесплатный PostgreSQL от Railway, Render или Supabase

---

## ✅ Рекомендация

**Для начала используйте Railway** - это самый простой и надежный вариант с хорошим бесплатным тарифом.



